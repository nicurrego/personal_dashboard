/**
 * @deprecated Import types from '@/types' instead of '@/lib/types'
 * This file exists for backward compatibility only.
 * 
 * All types have been moved to the /types directory:
 * - import type { Expense, Budget } from '@/types';
 * - import type { FilterState } from '@/types';
 * - import type { MonthlyData, TrendDataPoint } from '@/types';
 */

// Re-export all types for backward compatibility
export * from '@/types';
